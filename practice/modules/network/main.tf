# TODO(L1): variables.tf 의 입력과 outputs.tf 의 출력에 맞게
#   VPC + IGW + subnet(for_each) + route table + association 을 작성하세요.
#   힌트: week4 network.tf 를 모듈 변수(var.vpc_cidr, var.subnet_cidrs, var.name_prefix, var.tags)로 일반화.
